<%= render 'partials/merchant_navbar'%>

<div class="container">
    <div class="row header">
        <div class="col-lg-12 text-center">
            <h1>Bulk Discount: <%= @discount.name %></h1>
        </div>
    </div>
    <div class = "row"></div>
        <div class="col d-flex justify-content-center">
            <p>
                Threshold: <%= @discount.threshold %> <br>
                Discount Percentage: <%= @discount.discount %> <br>
                <%= link_to("Edit Bulk Discount", edit_merchant_bulk_discount_path(@merchant, @discount))%><br>
                <br>
            </p>
        </div>
        <%= link_to("Back", url_for(:back))%>
    </div>
    
</div>
